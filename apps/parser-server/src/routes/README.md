# Routes Module Boundary

Current scaffold exposes only `GET /health`.

Future Website Import work will add `POST /v1/imports/analyze` after URL security validation and Browser Manager boundaries are implemented.
