{
  "name": "@aio/browser-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node scripts/bundle-content-script.mjs && node scripts/copy-static.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "@aio/shared-contracts": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
